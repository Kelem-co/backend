

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.10.1 -->
<!-- Updated: 2026-05-10T20:00:42.897Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
config/asgi.py ← django, config
config/celery_app.py ← celery
core/conftest.py ← __future__, core, pytest
core/contrib/sites/migrations/0001_initial.py ← django
core/contrib/sites/migrations/0002_alter_domain_unique.py ← django
core/contrib/sites/migrations/0003_set_site_domain_and_name.py ← django
core/contrib/sites/migrations/0004_alter_options_ordering_domain.py ← django
core/users/adapters.py ← __future__, allauth, django
core/users/admin.py ← allauth, django, forms, models
core/users/api/serializers.py ← rest_framework, core
core/users/api/views.py ← rest_framework, core, serializers
core/users/apps.py ← django
core/users/context_processors.py ← django
core/users/forms.py ← allauth, django, models
core/users/migrations/0001_initial.py ← django, core
core/users/models.py ← django
core/users/tasks.py ← celery, models
core/users/tests/api/test_openapi.py ← django, pytest
core/users/tests/api/test_urls.py ← __future__, django
core/users/tests/api/test_views.py ← __future__, rest_framework, core, pytest
core/users/tests/factories.py ← __future__, factory, core
core/users/tests/test_admin.py ← importlib, django, pytest_django, core, pytest
core/users/tests/test_forms.py ← __future__, django, core
core/users/tests/test_models.py ← __future__
core/users/tests/test_tasks.py ← celery, core, pytest
core/users/tests/test_urls.py ← __future__, django
core/users/tests/test_views.py ← __future__, django, core, pytest
core/users/views.py ← __future__, django, core
media/admin.py ← __future__, django, models
media/api/serializers.py ← __future__, rest_framework, media
media/api/views.py ← __future__, django, rest_framework, media, serializers
media/apps.py ← django
media/migrations/0001_initial.py ← django
media/models.py ← __future__, django
media/storage.py ← __future__, botocore, django, boto3
media/tasks.py ← __future__, celery, django, config, media
media/tests/api/test_views.py ← __future__, unittest, rest_framework, core, media
media/tests/conftest.py ← __future__, django
media/tests/factories.py ← __future__, factory, core, media
media/tests/test_models.py ← __future__, django, media, pytest
media/tests/test_tasks.py ← __future__, unittest, django, media, pytest
```

## config

### config/asgi.py
```
async def application(scope, receive, send)
```

### config/celery_app.py
```
def config_loggers(*args, **kwargs)
```

### config/websocket.py
```
async def websocket_application(scope, receive, send)
```

## core

### core/conftest.py
```
def user(db) → User
```

### core/contrib/sites/migrations/0001_initial.py
```
class Migration(migrations.Migration)
```

### core/contrib/sites/migrations/0002_alter_domain_unique.py
```
class Migration(migrations.Migration)
```

### core/contrib/sites/migrations/0003_set_site_domain_and_name.py
```
class Migration(migrations.Migration)
def update_site_forward(apps, schema_editor)  # Set site domain and name
def update_site_backward(apps, schema_editor)  # Revert site domain and name to default
```

### core/contrib/sites/migrations/0004_alter_options_ordering_domain.py
```
class Migration(migrations.Migration)
```

### core/static/css/project.css
```
.alert-debug
.alert-error
```

### core/templates/base.html
```
title: {% block title %}
        core
      {% endblock title %}
div#navbarSupportedContent
```

### core/users/adapters.py
```
class AccountAdapter(DefaultAccountAdapter)
  def is_open_for_signup(request: HttpRequest) → bool
class SocialAccountAdapter(DefaultSocialAccountAdapter)
```

### core/users/admin.py
```
class UserAdmin(auth_admin.UserAdmin)
```

### core/users/api/serializers.py
```
class UserSerializer(serializers.ModelSerializer[User])
```

### core/users/api/views.py
```
class UserViewSet(RetrieveModelMixin, ListModelMixin, UpdateModelMixin, GenericViewSet)
  def get_queryset(*args, **kwargs)
  def me(request)
```

### core/users/apps.py
```
class UsersConfig(AppConfig)
  def ready()
```

### core/users/context_processors.py
```
def allauth_settings(request)  # Expose some settings from django-allauth in templates
```

### core/users/forms.py
```
class UserAdminChangeForm(admin_forms.UserChangeForm)
class UserAdminCreationForm(admin_forms.AdminUserCreationForm)
class UserSignupForm(SignupForm)
class UserSocialSignupForm(SocialSignupForm)
```

### core/users/migrations/0001_initial.py
```
class Migration(migrations.Migration)
```

### core/users/models.py
```
class User(AbstractUser)
  def get_absolute_url() → str
```

### core/users/tasks.py
```
def get_users_count()  # A pointless Celery task to demonstrate usage
```

### core/users/tests/api/test_openapi.py
```
def test_api_docs_accessible_by_admin(admin_client)
def test_api_docs_not_accessible_by_anonymous_users(client)
def test_api_schema_generated_successfully(admin_client)
```

### core/users/tests/api/test_urls.py
```
def test_user_detail(user: User)
def test_user_list()
def test_user_me()
```

### core/users/tests/api/test_views.py
```
class TestUserViewSet
  def api_rf() → APIRequestFactory
  def test_get_queryset(user: User, api_rf: APIRequestFactory)
  def test_me(user: User, api_rf: APIRequestFactory)
```

### core/users/tests/factories.py
```
class UserFactory(DjangoModelFactory[User])
  def password(: User, create: bool, extracted: str | None, **kwargs)
```

### core/users/tests/test_admin.py
```
class TestUserAdmin
  def test_changelist(admin_client)
  def test_search(admin_client)
  def test_add(admin_client)
  def test_view_user(admin_client)
  def test_allauth_login(rf, settings)
```

### core/users/tests/test_forms.py
```
class TestUserAdminCreationForm
  def test_username_validation_error_msg(user: User)
```

### core/users/tests/test_models.py
```
def test_user_get_absolute_url(user: User)
```

### core/users/tests/test_tasks.py
```
def test_user_count(settings)  # A basic test to execute the get_users_count Celery task
```

### core/users/tests/test_urls.py
```
def test_detail(user: User)
def test_update()
def test_redirect()
```

### core/users/tests/test_views.py
```
class TestUserUpdateView
  def dummy_get_response(request: HttpRequest)
  def test_get_success_url(user: User, rf: RequestFactory)
  def test_get_object(user: User, rf: RequestFactory)
  def test_form_valid(user: User, rf: RequestFactory)
class TestUserRedirectView
  def test_get_redirect_url(user: User, rf: RequestFactory)
class TestUserDetailView
  def test_authenticated(user: User, rf: RequestFactory)
  def test_not_authenticated(user: User, rf: RequestFactory)
```

### core/users/views.py
```
class UserDetailView(LoginRequiredMixin, DetailView)
class UserUpdateView(LoginRequiredMixin, SuccessMessageMixin, UpdateView)
  def get_success_url() → str
  def get_object(queryset: QuerySet | None) → User
class UserRedirectView(LoginRequiredMixin, RedirectView)
  def get_redirect_url() → str
```

## media

### media/admin.py
```
class MediaFileAdmin(admin.ModelAdmin)
  def has_add_permission(request) → bool
  def has_delete_permission(request, obj) → bool
class UploadFingerprintAdmin(admin.ModelAdmin)
  def has_add_permission(request) → bool
  def has_delete_permission(request, obj) → bool
  def has_change_permission(request, obj) → bool
```

### media/api/serializers.py
```
class UploadInitSerializer(serializers.Serializer)
  def validate_file_name(value: str) → str
class MediaFileSerializer(serializers.ModelSerializer)
  def get_download_url(obj: MediaFile) → str | None
class ConfirmUploadSerializer(serializers.Serializer)
class UploadResponseSerializer(serializers.Serializer)
class ConfirmResponseSerializer(serializers.Serializer)
```

### media/api/views.py
```
class MediaUploadViewSet(ViewSet)
  def upload(request: Request) → Response
  def confirm(request: Request, pk: str | None) → Response
  def retrieve(request: Request, pk: str | None) → Response
  def get_download_url(request: Request, pk: str | None) → Response
  def destroy(request: Request, pk: str | None) → Response
```

### media/apps.py
```
class MediaConfig(AppConfig)
  def ready() → None
```

### media/migrations/0001_initial.py
```
class Migration(migrations.Migration)
```

### media/models.py
```
class StatusChoices(models.TextChoices)
class MediaFile(models.Model)
  def is_stale(ttl_seconds: int) → bool
class UploadFingerprint(models.Model)
```

### media/storage.py
```
class S3StorageClient
  def __init__() → None
  def head_object(key: str) → dict[str, Any]
  def delete_object(key: str) → bool
  def delete_objects(keys: list[str]) → dict[str, Any]
```

### media/tasks.py
```
def setup_periodic_tasks(sender, **kwargs)
def cleanup_orphaned_uploads() → dict
```

### media/tests/api/test_views.py
```
class TestUploadInitiation
  def test_upload_requires_authentication(api_client)
  def test_upload_creates_media_file(authenticated_client, authenticated_user)
  def test_upload_validates_file_name(authenticated_client)
  def test_upload_sanitizes_file_name(authenticated_client)
class TestUploadConfirmation
  def test_confirm_requires_authentication(api_client)
  def test_confirm_verifies_object_exists(authenticated_client, authenticated_user)
class TestMediaRetrieval
  def test_retrieve_requires_authentication(api_client)
  def test_retrieve_self_scoped(authenticated_client, authenticated_user)
class TestDownloadUrl
  def test_download_url_requires_authentication(api_client)
class TestMediaDeletion
  def test_delete_requires_authentication(api_client)
def api_client()  # Return API client
def authenticated_user()  # Return authenticated user
def authenticated_client(api_client, authenticated_user)  # Return authenticated API client
```

### media/tests/conftest.py
```
def pytest_configure()  # Configure pytest for media tests
```

### media/tests/factories.py
```
class MediaFileFactory(DjangoModelFactory[MediaFile])
class UploadFingerprintFactory(DjangoModelFactory[UploadFingerprint])
```

### media/tests/test_models.py
```
class TestMediaFile
  def test_create_media_file() → None
  def test_key_uniqueness() → None
  def test_is_stale() → None
class TestUploadFingerprint
  def test_create_fingerprint() → None
  def test_fingerprint_uniqueness() → None
  def test_fingerprint_cascade_delete() → None
class TestStatusChoices
  def test_status_choices_values() → None
  def test_media_default_status() → None
```

### media/tests/test_tasks.py
```
class TestCleanupOrphanedUploads
  def test_cleanup_deletes_pending_uploads_older_than_ttl()
  def test_cleanup_deletes_failed_uploads()
  def test_cleanup_deletes_objects_from_storage()
  def test_cleanup_respects_ttl_setting()
  def test_cleanup_handles_errors_gracefully()
  def test_cleanup_ignores_recent_uploads()
```
