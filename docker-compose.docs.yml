services:
  docs:
    image: kelem_co_backend_local_docs
    container_name: kelem_co_backend_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./kelem_co_backend:/app/kelem_co_backend:z
    ports:
      - '9000:9000'
    command: /start-docs
